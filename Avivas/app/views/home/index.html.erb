<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home</title>
  
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
  
  <%= javascript_include_tag 'application', 'data-turbo-track': 'reload', defer: true %>

<style>


.navbar-brand { font-size: 1.8rem; font-weight: bold; } 

.nav-link { font-size: 1.2rem; margin-right: 10px; } 

.btn-link { font-size: 1.2rem; margin-right: 10px; color: #ff0000; } 

.container-fluid { background-color: #f8f9fa; } /* Estilo para el buscador y el filtro */ 

form { margin: 20px 0; } 

input[type="text"] { padding: 10px; width: 70%; border: 1px solid #ccc; border-radius: 5px; margin-right: 10px; } 

button { padding: 10px 20px; border: none; background-color: #007bff; color: white; border-radius: 5px; cursor: pointer; } 

button:hover { background-color: #0056b3; } 

select { padding: 10px; width: 70%; border: 1px solid #ccc; 
border-radius: 5px; 
margin-right: 10px; }

.productos {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
}

.producto {
  border: 1px solid #ddd;
  padding: 10px;
  width: 30%;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
}

.producto img {
  width: 100%;
  height: auto;
}

.producto h3 {
  font-size: 1.5rem;
}

.producto p {
  margin: 0.5rem 0;
}
</style>
 
</head>
<body>
  <!-- Mensajes Flash -->
  <% if flash[:notice] %>
    <div class="alert alert-success">
      <%= flash[:notice] %>
    </div>
  <% end %>

  <!-- Barra de navegación -->
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Avivas</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <% if user_signed_in? %>
            <li class="nav-item">
              <%= link_to "Editar perfil", edit_user_registration_path, class: "nav-link active", aria_current: "page" %>
            </li>
            <li class="nav-item">
              <%= link_to "Usuarios", users_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Productos", productos_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Ventas", ventas_path, class: "nav-link" %>
            </li>

            <li class="nav-item">
              <%= button_to "Cerrar sesión", destroy_user_session_path, method: :delete, data: { confirm: '¿Estás seguro que deseas cerrar sesión?' }, class: "btn btn-link" %>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to "Iniciar sesión", new_user_session_path, class: "nav-link" %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Contenido principal -->
  <main class="container mt-4">
    <%= yield %>

    <!-- Buscador -->
    <form method="get" action="<%= root_path %>">
      <input type="text" name="q" placeholder="Buscar productos..." value="<%= params[:q] %>">
      <button type="submit">Buscar</button>
    </form>

    <!-- Filtro de categoría -->
    <%= form_with url: home_por_categoria_path, method: :get, local: true do %>
      <%= select_tag :categoria, options_from_collection_for_select(@categorias, 'id', 'nombre'), include_blank: 'Selecciona una categoría' %>
      <%= submit_tag 'Filtrar' %>
    <% end %>
  </main>
  
  <!-- Lista de productos -->
  <div class="productos">
    <% if @productos.present? %>
      <% @productos.each do |producto| %>
        <div class="producto">
          <% if producto.imagenes.any? %> <!-- Comprobamos si tiene imágenes -->
            <div class="imagenes-producto">
              <% producto.imagenes.each do |imagen| %> <!-- Iteramos sobre todas las imágenes -->
                <%= image_tag imagen.url, alt: producto.nombre, class: "img-fluid" %>
              <% end %>
            </div>
          <% end %>
          <h3><%= producto.nombre %></h3>
          <p><%= producto.descripcion %></p>
          <p><strong>Precio:</strong> $<%= producto.precio %></p>
          <p><strong>Stock disponible:</strong> <%= producto.stock %></p>
        </div>
      <% end %>
    <% else %>
      <p>No hay productos disponibles</p>
    <% end %>
  </div>
  
</body>

</html>
